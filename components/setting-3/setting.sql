
CREATE TABLE  `setting` (
`slug` VARCHAR( 255 ) NOT NULL ,
`name` VARCHAR( 255 ) NOT NULL ,
`text` TEXT NOT NULL ,
`cat` VARCHAR( 255 ) NOT NULL ,
PRIMARY KEY (  `slug` )
) ENGINE = MYISAM;


--spi--
